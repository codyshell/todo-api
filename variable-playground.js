/* var person = {
	name: "Alpha",
	age: 30
};

function updatePerson(obj){
	// obj = {
		// name: "Alpha",
		// age: 25
	// };
	obj.age = 25;
}

updatePerson(person);
console.log(person); */

var grades = [14, 24];

function addGrades(gradesArr) {
	gradesArr.push(55);
	debugger;
	// gradesArr = [36, 13];
}

addGrades(grades);
console.log(grades);